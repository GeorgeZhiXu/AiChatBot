# 🤖 AI Group Chat

一个实时多人群聊应用，集成了 DeepSeek AI 助手。用户可以通过 `@AI` 触发 AI 助手，所有群成员都能实时看到 AI 的流式响应。

## ✨ 功能特性

- 💬 **实时群聊** - 多用户实时消息同步
- 🤖 **AI 助手** - 通过 `@AI` 触发 DeepSeek AI，支持流式响应
- 🌊 **流式显示** - AI 回复逐字显示，提供打字机效果
- 👥 **用户管理** - 用户加入/离开通知，在线人数统计
- 📜 **消息历史** - 保存最近 500 条消息，刷新后自动恢复
- 📱 **跨设备支持** - 局域网内任意设备可访问
- 🎨 **现代化界面** - 深色主题，响应式设计

## 🏗️ 技术栈

### 后端
- **FastAPI** - 高性能 Python Web 框架
- **Socket.IO** - WebSocket 实时通信
- **DeepSeek API** - AI 对话能力
- **Python 3.12** - 编程语言

### 前端
- **React 19** - UI 框架
- **Vite** - 构建工具
- **Socket.IO Client** - WebSocket 客户端
- **Tailwind CSS** - 样式框架

## 📦 项目结构

```
AiChatBot/
├── backend/              # 后端服务
│   ├── main.py          # FastAPI + Socket.IO 主程序
│   ├── deepseek_client.py  # DeepSeek AI 客户端
│   ├── requirements.txt    # Python 依赖
│   └── .env.example        # 环境变量模板
│
├── frontend/            # 前端应用
│   ├── src/
│   │   ├── App.jsx            # 主应用组件
│   │   ├── hooks/
│   │   │   └── useSocket.js   # Socket.IO Hook
│   │   └── index.css          # 全局样式
│   └── package.json           # Node 依赖
│
└── README.md           # 项目文档
```

## 🚀 快速开始

### 前置要求

- Python 3.12+
- Node.js 16+
- DeepSeek API Key（从 https://platform.deepseek.com 获取）

### 1. 配置后端

```bash
# 进入后端目录
cd backend

# 创建虚拟环境（如果不存在）
python3 -m venv .venv

# 激活虚拟环境
source .venv/bin/activate  # macOS/Linux
# 或
.venv\Scripts\activate     # Windows

# 安装依赖
pip install -r requirements.txt

# 配置环境变量
cp .env.example .env
# 编辑 .env 文件，添加你的 DeepSeek API Key
```

### 2. 配置前端

```bash
# 进入前端目录
cd frontend

# 安装依赖
npm install
```

### 3. 启动应用

**启动后端（终端 1）：**
```bash
cd backend
source .venv/bin/activate
export DEEPSEEK_API_KEY="your-api-key-here"
uvicorn main:asgi_app --host 0.0.0.0 --port 8000 --reload
```

**启动前端（终端 2）：**
```bash
cd frontend
npm run dev -- --host
```

### 4. 访问应用

- **本机访问**：http://localhost:5173
- **局域网访问**：http://你的IP:5173（如 http://192.168.1.22:5173）

## 📖 使用说明

### 基本聊天

1. 打开应用，输入用户名加入聊天室
2. 在输入框输入消息，按 Enter 或点击 Send 发送
3. 所有在线用户都能实时看到消息

### 使用 AI 助手

在消息中使用 `@AI` 或 `@ai` 触发 AI 助手：

```
@AI 你好，请介绍一下自己
```

```
@ai 帮我规划一个三亚7天旅行行程
```

**特性：**
- ✨ AI 回复会流式显示（逐字出现）
- 👥 所有用户都能看到 AI 响应
- 🧠 AI 能理解聊天上下文（最近 10 条消息）
- ⚡ 支持队列处理，多个 @AI 请求会依次执行

## 🎯 Socket.IO 事件

### 客户端 → 服务器

| 事件 | 数据 | 说明 |
|------|------|------|
| `user_join` | `{username: string}` | 用户加入聊天室 |
| `chat_message` | `{message: string}` | 发送聊天消息 |

### 服务器 → 客户端

| 事件 | 数据 | 说明 |
|------|------|------|
| `user_joined` | `{username, user_count, timestamp}` | 新用户加入通知 |
| `user_left` | `{username, user_count, timestamp}` | 用户离开通知 |
| `chat_history` | `{messages: Message[]}` | 历史消息（新用户加入时） |
| `chat_message` | `{id, username, content, timestamp, is_ai}` | 新聊天消息 |
| `ai_response_start` | `{id, username, triggered_by}` | AI 开始响应 |
| `ai_response_chunk` | `{id, chunk: string}` | AI 流式内容块 |
| `ai_response_end` | `{id}` | AI 响应完成 |
| `ai_busy` | `{message: string}` | AI 正在处理其他请求 |
| `error` | `{message: string}` | 一般错误 |
| `ai_error` | `{message: string}` | AI 调用错误 |

## 🔧 配置

### 环境变量

**backend/.env：**
```env
DEEPSEEK_API_KEY=your-api-key-here
DEEPSEEK_API_BASE=https://api.deepseek.com
```

### 修改端口

**后端端口（默认 8000）：**
```bash
uvicorn main:asgi_app --host 0.0.0.0 --port 8888
```

**前端端口（默认 5173）：**
修改 `frontend/vite.config.js` 或使用：
```bash
npm run dev -- --port 3000 --host
```

## 🐛 常见问题

### Q: 其他电脑无法访问？
**A:** 检查以下几点：
1. 前端启动时是否使用了 `--host` 参数
2. 防火墙是否允许端口 5173 和 8000
3. 确保在同一局域网内

### Q: AI 不响应或报错？
**A:** 检查：
1. `DEEPSEEK_API_KEY` 是否正确设置
2. 后端日志中是否有错误信息
3. API Key 是否有效且有额度

### Q: 消息历史在哪里存储？
**A:** 当前版本使用内存存储，服务器重启后历史记录会丢失。如需持久化，可以：
- 集成 SQLite 数据库
- 使用 Redis 存储
- 添加文件日志

## 📈 性能指标

- **响应延迟**：< 100ms（普通消息）
- **AI 首字延迟**：< 2 秒
- **支持并发**：50+ 用户
- **消息历史**：最近 500 条

## 🛣️ 未来计划

- [ ] 数据库持久化聊天记录
- [ ] 用户认证和权限管理
- [ ] 多聊天室/频道功能
- [ ] 私聊功能
- [ ] 文件/图片分享
- [ ] @用户名 提及功能
- [ ] AI 参数配置（temperature, model 选择）
- [ ] 消息编辑/删除
- [ ] Emoji 支持

## 📝 更新日志

### v1.0.0 (2025-12-03)
- ✨ 初始版本发布
- ✅ 实时群聊功能
- ✅ @AI 触发 DeepSeek 助手
- ✅ AI 流式响应
- ✅ 用户管理和消息历史
- ✅ 跨设备访问支持

## 📄 License

MIT License

## 👨‍💻 作者

- **Vivian** - 项目开发者

## 🙏 致谢

- [FastAPI](https://fastapi.tiangolo.com/) - 现代化 Python Web 框架
- [Socket.IO](https://socket.io/) - 实时通信库
- [DeepSeek](https://www.deepseek.com/) - AI 对话能力
- [React](https://react.dev/) - 前端 UI 框架
- [Vite](https://vitejs.dev/) - 快速构建工具
